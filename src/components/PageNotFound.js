import React from "react";

const PageNotFound = () => (
  <>
    <h1>Page not found.</h1>
    <h4>
      You are either not authorized to view this page or the requested url is
      invalid
    </h4>
  </>
);

export default PageNotFound;
